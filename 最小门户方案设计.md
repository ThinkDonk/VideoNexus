# 最小门户方案设计(方案 B 精简版)

## 一、背景

**业务背景**：我方为仓库(质押物监管)方，仓库内摄像头已通过 GB28181 协议注册到 WVP + ZLM 平台，实时预览和录像回放功能已验证可用。现需向第三方银行开放访问：银行监管人员通过网页登录我方平台，**只能查看被授权的摄像头**，可实时观看、按时间段回放，并下载录像，用于贷后巡库与风险核查。

**技术背景**：WVP 的管理端是面向内部运维的工具，自带 RBAC 是菜单/API 级的，不满足对外场景的三个硬性要求——**通道级数据权限**(不同银行只看各自的仓库)、**面向外部账号的独立体系**(不能给银行开 WVP 管理端账号)、**可提交给银行合规审查的详细审计日志**。

**方案定位**：门户系统只做“账号、授权、审计、下载编排”四件事，**视频能力(信令、拉流、转协议)完全复用现有 WVP/ZLM**,不重复造轮子。WVP 保持原样、仅内网访问，门户未来可随 WVP 版本升级而互不影响。

## 二、需求与范围边界

**功能性要求**

| 编号 | 要求 |
|---|---|
| R1 | 银行账号由我方管理员手工创建，不开放自助注册；账号归属某家银行(机构) |
| R2 | 授权粒度：用户 ↔ 具体摄像头通道，动作分**实时 / 回放 / 下载**三种，带有效期 |
| R3 | 实时预览(H5 播放)、按时间段回放、按时间段下载录像 |
| R4 | 全程审计：登录、播放、回放、下载、授权变更均留痕，可查询、可导出，保留 ≥6 个月 |

**非功能要求**：全程 HTTPS;所有视频操作服务端校验授权(防横向越权)；播放地址不可转发外泄；播放页叠加水印；审计日志只增不删。

**第一期明确不做(最小化边界)**：下载审批工作流、SSO/双因子、多级组织架构、高可用部署、大规模并发(设置上限即可)。数据模型预留机构字段，未来多家银行接入不用重构。

## 三、总体架构

```
银行用户浏览器
     │ HTTPS(唯一公网入口)
     ▼
┌─────────────────────────────┐
│ Nginx:TLS、静态资源、反向代理  │
├─────────────────────────────┤
│ 门户后端(BFF)                │
│ ·账号/授权/审计/下载任务       │
│ ·是 WVP 的唯一调用者          │
│ 门户数据库(独立于 WVP 库)     │
└──────────┬──────────────────┘
           │ REST(内网,管理员令牌)
           ▼
   WVP(仅内网)── ZLM(流媒体,仅内网)

播放流:ZLM → Nginx 代理 + 签名短时效 URL → 浏览器
下载文件:门户生成的录像文件 → 限时签名直链 + 限速
```

**核心原则**：WVP 管理界面和 ZLM 端口一律不对公网暴露，银行用户的所有请求都经门户后端转发；门户是 WVP 的唯一 API 调用方(持有 WVP 管理员令牌)。

## 四、核心设计要点

**1. 授权模型(数据权限)**
门户侧维护“用户—通道”授权表。通道基础数据(设备/通道列表)从 WVP 查询(可缓存定时同步)，门户按当前用户的授权过滤后生成摄像头树。任何点播、回放、下载请求，门户后端**必须**先查该授权表——校验放在服务端，不依赖前端隐藏。

**2. 播放链路**
用户点通道 → 门户校验授权 → 调 WVP 点播/回放接口获得流地址 → 门户改写为**带签名、短时效、绑定会话**的地址，经 Nginx 代理下发 → 前端播放器播放并叠加动态水印(用户名+时间)。播放结束事件以前端上报为主、ZLM 的无人观看回调兜底，写入审计(含观看时长、流量)。

**3. 下载链路(异步任务)**
下载不可能同步完成，做成任务：用户提交通道+时间段 → 门户校验下载权限并创建任务 → 后端拉取 WVP 回放流、落盘为 MP4 文件 → 完成后生成限时下载直链(限速)通知用户 → 文件保留 N 天后自动清理，存储设配额。任务全生命周期(申请/开始/完成/失败、文件大小、时间段)全部入审计。

**4. 审计设计(独立于 WVP 日志)**

| 事件类别 | 必记内容 |
|---|---|
| 登录类 | 成功/失败、IP、UA、登出、改密、连续失败锁定 |
| 视频类 | 播放开始/结束(通道、时长)、回放(时间段)、下载(时间段、文件、大小、成败) |
| 授权类 | **谁把哪路视频授权给谁/收回，有效期**——追责时最关键 |
| 管理类 | 用户创建/停用、异常(如签名 URL 校验失败，可能是地址外泄) |

审计表设计为只追加，应用账号不授予删改权限；每日备份，可按人/机构/通道/时间/动作检索并导出 CSV(应对银行合规抽查)。

## 五、数据概念模型

| 实体 | 用途 | 关键属性 |
|---|---|---|
| 机构 | 一家银行 | 名称、状态、可选 IP 白名单 |
| 用户 | 银行人员 / 我方管理员 / 审计员 | 所属机构、角色、密码哈希、锁定状态 |
| 通道授权 | 核心数据权限 | 用户、通道标识、实时/回放/下载三个开关、有效期、授权人 |
| 播放会话 | 一次点播/回放 | 通道、类型、起止时间、时长 |
| 下载任务 | 录像下载 | 通道、时间段、状态、文件路径、大小、发起人 |
| 审计日志 | 全部事件 | 人、动作、对象、参数、结果、IP、UA、时间 |

## 六、部署形态(最小)

- **新增 1 台应用服务器**：Nginx + 门户后端 + 门户数据库 + 下载文件临时存储(带配额与自动清理)
- **现有 WVP + ZLM 服务器不动**，只需收敛端口：对公网只开放 Nginx 的 443;ZLM 流端口改走内网经代理分发
- 门户数据库每日备份；审计数据另行定期导出归档

## 七、分期计划

| 阶段 | 周期 | 内容 |
|---|---|---|
| 第 0 期·验证 | 约 1 周 | 脚本验证 WVP API:通道列表、点播取流、回放、录像查询、**回放流拉取落盘**(下载可行性，长时段耗时评估)；确认 ZLM 播放地址签名校验方式；**与银行确认审计字段清单和导出格式** |
| 第 1 期·MVP | 4~6 周 | 账号 + 通道授权 + 摄像头树 + 实时 + 回放 + 下载任务 + 审计 + HTTPS + 水印 + 登录锁定 |
| 第 2 期 | 按需 | 下载审批流、双因子/SSO、审计报表自动报送、日志哈希防篡改、按机构统计报表 |
| 第 3 期 | 可选 | 高可用、多机房 |

## 八、风险与待确认问题

1. **下载耗时与带宽**：长时段录像转文件耗时长且占出口带宽，MVP 应限制单次下载时长上限、并发下载数、限速，并排队处理
2. **录像存储位置**：需确认录像在设备 NVR 还是平台侧，影响下载实现方式
3. **银行合规硬性要求**：是否要求等保测评、专线接入、日志定期报送——这三项任何一项都会显著加长工期，须提前书面确认
4. **基础数据治理**：授权的前提是通道按“仓库/库区/点位”整理清楚并稳定编号，建议 MVP 前先做一轮通道台账梳理

**总结一句话**：这套最小方案 = 一台新服务器 + 一个只管“账号、授权、审计、下载任务”的轻门户，WVP 原样内网化，播放走签名地址，下载走异步落盘，审计独立成表且不可改——四到六周可上线第一版，后续审批、双因子等按银行反馈渐进叠加。